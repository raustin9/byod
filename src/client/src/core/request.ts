

export class BYODRequest {
    address: string;
    radius: number;

    constructor(address: string, radius: number) {
        this.address = address;
        this.radius = radius;
    }

    // Send the request to the server
    send() {
        // TODO: 
    }
}
